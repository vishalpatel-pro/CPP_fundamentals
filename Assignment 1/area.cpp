//  Write a program to calculate the area of a rectangle.

#include <iostream>
using namespace std;
int main()
{
    float length, width;
    cin >> length >> width;
    cout << "Area = " << length * width;
    return 0;
}